/// Keys used to persist authentication data in the local Hive box.
class StorageKeys {
  StorageKeys._();

  static const String idToken = 'id_token';
  static const String refreshToken = 'refresh_token';
}
